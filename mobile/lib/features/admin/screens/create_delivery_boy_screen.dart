import 'package:flutter/material.dart';

class CreateDeliveryBoyScreen extends StatelessWidget {
  const CreateDeliveryBoyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add Delivery Boy')),
      body: const Center(child: Text('Create Delivery Boy form coming soon')),
    );
  }
}
